#include "main.h"

/**
 * _strspn - a function that gets the length of a prefix substring
 * @s: string that consists of bytes from accept
 * @accept: only one in s
 *
 * Return: n
 */
unsigned int _strspn(char *s, char *accept)
{
}
